# NBA 2025 Terminal Seed Assets

This directory is populated locally by `tools/sync_nba_terminal_assets.py` or by the full `tools/run_nba_terminal_data_pipeline.py` pipeline.

The generated JSON files are intentionally not handwritten. Rebuild the local warehouse from the completed Basketball Reference raw catalog, export the terminal seed, validate it, and sync it here before running the Flutter app.
