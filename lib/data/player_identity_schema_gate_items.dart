import '../models/registry_item.dart';

const playerIdentitySchemaGateItems = <RegistryItem>[
  RegistryItem(id: 'player-id-required-id', title: 'Stable playerId', category: 'Required Field', priority: 'P0', status: 'Required before import', description: 'Every player row needs one durable internal playerId that all future stats, awards, rosters, draft rows, games, and reports can join to.', inputs: 'player_profiles.json, provider ID mapping', nextStep: 'Define canonical ID format before importing rows.'),
  RegistryItem(id: 'player-id-display-name', title: 'Display name', category: 'Required Field', priority: 'P0', status: 'Required before import', description: 'Every player row needs a user-facing display name for search, tables, reports, comparison slots, and route payload labels.', inputs: 'source export, player profile model', nextStep: 'Keep displayName separate from firstName and lastName.'),
  RegistryItem(id: 'player-id-name-parts', title: 'First and last name fields', category: 'Required Field', priority: 'P0', status: 'Required before import', description: 'Name parts should be available when source-backed, but displayName remains the primary UI label.', inputs: 'source export, player profile model', nextStep: 'Allow blanks only when the source does not provide a clean split.'),
  RegistryItem(id: 'player-id-source-id', title: 'Source ID and as-of metadata', category: 'Required Field', priority: 'P0', status: 'Required before import', description: 'Every identity row needs sourceId and asOf metadata so downstream reports and exports preserve trust context.', inputs: 'Source Registry, Data Lineage, player_profiles.json', nextStep: 'Attach sourceId and asOf to every imported row.'),
  RegistryItem(id: 'player-id-active-status', title: 'Active status policy', category: 'Optional Field', priority: 'P0', status: 'Decision needed', description: 'Active or retired status is useful but must be source-backed or clearly blank.', inputs: 'source export, player profile model', nextStep: 'Decide whether active status is required for first import or optional.'),
  RegistryItem(id: 'player-id-birth-fields', title: 'Birthdate and birthplace policy', category: 'Optional Field', priority: 'P1', status: 'Optional', description: 'Birth fields can improve reports and search but should not block the first identity import.', inputs: 'source export, player profile model', nextStep: 'Keep optional until a reliable source path is chosen.'),
  RegistryItem(id: 'player-id-position-policy', title: 'Position field policy', category: 'Optional Field', priority: 'P1', status: 'Optional', description: 'Position can vary by source and season, so first import should avoid pretending it is always exact.', inputs: 'source export, roster entries later', nextStep: 'Prefer season roster position later if source coverage is stronger there.'),
  RegistryItem(id: 'player-id-alias-table', title: 'Alias table required', category: 'Join Safety', priority: 'P0', status: 'Required before stats', description: 'Alias rows are needed for spelling variants, suffixes, provider IDs, historical names, and duplicate-name review.', inputs: 'player_aliases.json, provider ID mapping', nextStep: 'Create alias table after initial player identity source is selected.'),
  RegistryItem(id: 'player-id-duplicate-check', title: 'Duplicate player check', category: 'Validation', priority: 'P0', status: 'Required before import', description: 'The identity import must reject duplicate internal playerId values and flag duplicate display names for review.', inputs: 'validation script, player_profiles.json', nextStep: 'Add validation expectations before importing rows.'),
  RegistryItem(id: 'player-id-join-check', title: 'Join readiness check', category: 'Validation', priority: 'P0', status: 'Required before stats', description: 'Player stats, awards, rosters, draft, and game logs should not load until they can join to player identity.', inputs: 'player_profiles.json, future stat imports', nextStep: 'Make player identity the first required dependency for player-centric imports.'),
  RegistryItem(id: 'player-id-null-policy', title: 'Blank/null policy', category: 'Validation', priority: 'P0', status: 'Required before import', description: 'Required fields cannot be blank; optional fields can be blank only with clear source-aware display behavior.', inputs: 'Field Dictionary, player profile model', nextStep: 'Update field dictionary expectations before import.'),
  RegistryItem(id: 'player-id-route-check', title: 'Route payload compatibility', category: 'Workflow', priority: 'P0', status: 'Required before import', description: 'Imported player rows must be able to generate RoutePayload objects for Search, Workspace, Compare, Reports, Saved Views, Export, Alerts, and Action Center.', inputs: 'RoutePayload, player profile rows, consumer registry', nextStep: 'Reuse Team and Season route behavior for player rows.'),
];
