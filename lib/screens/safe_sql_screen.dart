import 'package:flutter/material.dart';

class SafeSqlScreen extends StatelessWidget {
  const SafeSqlScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('SQL workspace');
  }
}
